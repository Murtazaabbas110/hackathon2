# Gemini JSON Client Code Gig

Server-side helper for calling the Gemini API and enforcing structured JSON responses.

Used for:
- Project analysis from a raw client message.

This module must only be imported from server-side (Next.js Route Handlers or Server Actions).
